# LITELLM_MASTER_KEY: admin key of the LiteLLM proxy (same value as litellm/.env)
LITELLM_MASTER_KEY=change_me
# LITELLM_MCP_TOKEN: bearer token agents use for this MCP server (generate: openssl rand -hex 32)
LITELLM_MCP_TOKEN=change_me