[Unit]
Description=M2Lab control-plane dashboard
After=docker.service network-online.target

[Service]
WorkingDirectory=@OMNILAB_ROOT@
ExecStart=@OMNILAB_ROOT@/.venv/bin/python -m ctl.app
Restart=on-failure
RestartSec=3
EnvironmentFile=-@OMNILAB_ROOT@/.env

[Install]
WantedBy=default.target
